using UnityEngine;
using System.Collections;

public class AutoCreate : MonoBehaviour {
	public GameObject zhangAi;
	public Transform createPos;
	// Use this for initialization
	void Start () {
	}
	
	// Update is called once per frame
	void Update () {
		if(BirdControll.isStart)
		{
		}
	}
}
